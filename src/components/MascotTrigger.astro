---
interface Props {
    id: string;
    ctaText: string;
}

const { id, ctaText } = Astro.props;
---

<button
    popovertarget={id}
    popovertargetaction="show"
    class="mascot-toggle-btn"
    aria-label={ctaText}
>
    <img src="uploads/icons/heart.svg" alt={ctaText} />
</button>
